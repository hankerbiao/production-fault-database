import { X } from 'lucide-react';
export function DetailPanel({ detail, loading, close, title = '记录详情', heading = '完整数据库字段' }) { return <div className="drawer-backdrop" onClick={close}><aside className="detail-panel" onClick={event => event.stopPropagation()}><div className="detail-head"><div><p className="eyebrow">{title}</p><h2>{loading ? '读取字段中' : heading}</h2></div><button className="icon-button" title="关闭" onClick={close}><X size={20} /></button></div>{loading ? <div className="detail-loading">正在读取完整字段...</div> : detail?.error ? <p className="empty">{detail.error}</p> : <div className="field-grid">{(detail?.fields || []).map(field => <div className="field" key={field.key}><span>{field.label}</span><strong>{field.value || '-'}</strong><small>{field.key}</small></div>)}</div>}</aside></div>; }
