import { ChevronLeft, ChevronRight } from 'lucide-react';
export function Pagination({ page, total, pageSize, setPage, bilingual = false }) { return <div className="pagination"><span>{total ? `${bilingual ? '显示 / Showing ' : '显示 '}${(page - 1) * pageSize + 1}-${Math.min(page * pageSize, total)} ${bilingual ? '条 / records' : '条'}` : (bilingual ? '暂无记录 / No records' : '暂无记录')}</span><div><button disabled={page === 1} onClick={() => setPage(page - 1)} title={bilingual ? '上一页 / Previous page' : '上一页'}><ChevronLeft size={17} /></button><b>{page}</b><button disabled={page * pageSize >= total} onClick={() => setPage(page + 1)} title={bilingual ? '下一页 / Next page' : '下一页'}><ChevronRight size={17} /></button></div></div>; }
